package com.sirolf2009.muse.interfaces

/**
 * Some thing that does something
 * Contains:
 *  - classpath (maybe don't call it path, class environment?)
 */
interface IOperator extends IDrawable {
	
}